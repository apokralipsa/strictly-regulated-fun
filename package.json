{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.22.1",
    "prettier": "^2.0.5",
    "typescript": "^3.9.6"
  },
  "scripts": {
    "build": "lerna run tsc && lerna bootstrap",
    "benchmark": "npm run build && lerna run benchmark"
  }
}
